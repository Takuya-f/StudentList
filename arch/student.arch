interface component Main{
	void actionPerformed(ActionEvent e);
}

interface component StudentController{
	void filterStudent(JTable table);
	void colorStudent(JTable table);
}

uncertain connector ucStudent extends Main{
	u Student = (Main.actionPerformed -> {StudentController.filterStudent, StudentController.colorStudent} -> Main);
}